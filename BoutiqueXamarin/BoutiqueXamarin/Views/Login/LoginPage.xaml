<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:menuItems="clr-namespace:BoutiqueXamarin.Views.ContentViews.MenuItems;assembly=BoutiqueXamarin"
             x:Class="BoutiqueXamarin.Views.Login.LoginPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <menuItems:LogoMenuView Grid.Row="0" />

        <StackLayout Grid.Row="1"
                     Style="{StaticResource StackLayoutControl}">
            <Entry x:Name="LoginEntry" 
                   Placeholder="Имя пользователя"/>
            <Entry x:Name="PasswordEntry"
                   IsPassword="True"
                   Placeholder="Пароль"/>

            <Button x:Name="LoginButton"
                    Style="{StaticResource ButtonMain}"
                    Text="Войти"/>
            <Button x:Name="RegisterButton"
                    Style="{StaticResource ButtonMain}"
                    Text="Регистрация"/>

            <Label Style="{StaticResource LabelCommon}"
                   Text="Забыли пароль?"/>
        </StackLayout>
    </Grid>
</ContentPage>